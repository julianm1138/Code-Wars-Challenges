Description:
Create a function that checks if a number n is divisible by two numbers x AND y. All inputs are positive, non-zero numbers.

// We need to return a boolean considering both equations. If both equations are true, return true, else return false.
// We can do this using implicit return and the && operator


function isDivisible(n, x, y) {
  
  return n % x === 0 && n % y === 0;
  
}
