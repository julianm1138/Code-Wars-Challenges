function sum (numbers) {
  
  let sum = 0;
  
  numbers.forEach((number)=> {
    
    if (typeof number === 'number') {
      sum += number
    }
    
  });
return sum
    
} 

//Instructions: Write a function that takes an array of numbers and returns the sum of the numbers. The numbers can be negative or non-integer. If the array does not contain any numbers then you should return 0.

Examples
Input: [1, 5.2, 4, 0, -1]
Output: 9.2

Input: []
Output: 0

Input: [-2.398]
Output: -2.398

//We initialize the sum and use forEach to iterate over the input array. 
//Conditions of the forEach: if the input is a number we add the number to the sum and then return the sum. Easy. 
