function numberToString(num) {
  
  const str = num.toString();
   {
     return str
   }
  // Return a string of the number here!
}


//I had to look this one up the first time, but thankfully we have a JS method to convert a number into a string. 
