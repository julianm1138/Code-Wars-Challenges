//The bug was a prototype being assigned to args and args was not defined in the function declaration. 


function swapValues(args) {
   
    var temp = args[0];
    args[0] = args[1];
    args[1] = temp;
}
