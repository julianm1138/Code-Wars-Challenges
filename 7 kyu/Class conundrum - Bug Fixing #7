//This was a debuggging challenge 

class List {
  constructor(type) {
    this.type = type;
    this.items = [];
    this.count = 0;
  }
  add(item) {
    if ( typeof item != this.type )
      return `This item is not of type: ${typeof item}`;

    this.items.push(item);
    return item;
  }
}
