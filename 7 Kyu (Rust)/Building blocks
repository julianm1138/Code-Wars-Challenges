The constructor should take an array as an argument, this will contain 3 integers of the form [width, length, height] from which the Block should be created.

// We can follow the directions to create the required struct.
// The struct creates a block.
// Make a constructor that takes an array as an argument ([width, length, height])
// Define the methods listed


struct Block {
    width: u32,
    length: u32, 
    height: u32,
}

impl Block {
    
    fn new(dimensions: &[u32; 3]) -> Self {
        Block {
        width: dimensions[0],
        length: dimensions[1],
        height: dimensions[2]
        }
    }
    
    
    fn get_width(&self) -> u32 {
        self.width
    }
     fn get_length(&self) -> u32 {
        self.length
    }
     fn get_height(&self) -> u32 {
        self.height
    }
    fn get_volume(&self) -> u32 {
         self.width * self.length * self.height
    }
    fn get_surface_area(&self) -> u32 {
        2 * self.length * self.width + 2 * self.length * self.height + 2 * self.width * self.height
    }
}


